version: 0.0
os: linux
files:
    - source: /build/libs/
      destination: /home/ubuntu/gamgyul-code-server/build/libs/
file_exists_behavior: OVERWRITE

permissions:
    - object: /build/libs/
      pattern: '**'
      owner: root
      group: root

hooks:
    AfterInstall:
        - location: scripts/after-deploy.sh
          timeout: 2000
          runas: root
